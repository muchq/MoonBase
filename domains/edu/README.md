# edu

Learning applications.

- **[`apps/ast_lol`](apps/ast_lol)** — [ast.lol](https://ast.lol): a tiered,
  auto-graded course on AST parsing and transformation, building to SQL query
  parsing and rule-based plan optimization. Client-side React SPA on
  Cloudflare Workers; the grader runs user JavaScript in a browser Web Worker.
