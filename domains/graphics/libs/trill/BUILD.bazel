load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "trill",
    srcs = ["trill.cc"],
    hdrs = ["trill.h"],
    # foreign_cc bootstrap is broken (#1524).
    tags = ["manual"],
    visibility = ["//visibility:public"],
    deps = [
        "@sdl3",
    ],
)
