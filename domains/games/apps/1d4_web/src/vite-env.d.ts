/// <reference types="vite/client" />

/** CHESSQL.md, rendered to HTML at build time — see vite-plugin-chessql.ts. */
declare module 'virtual:chessql-reference' {
  const html: string;
  export default html;
}
