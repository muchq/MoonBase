load("@crates//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "microgpt-train",
    srcs = [
        "src/main.rs",
    ],
    crate_name = "microgpt_train",
    deps = [
        "//domains/ai/libs/microgpt",
    ] + all_crate_deps(),
)
